language: python
python:
  - "3.9"
install:
  - pip install flask
  - pip install flask-sqlalchemy
  - pip install requests
  - pip install flask_login

# Run tests
script:
  - python -m pytest --cov

